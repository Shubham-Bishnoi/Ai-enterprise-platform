import { ContentPageTemplate } from '@/components/shared/ContentPageTemplate';
import { companyPage } from '@/lib/siteContent';

export default function Company() {
  return <ContentPageTemplate page={companyPage} />;
}
