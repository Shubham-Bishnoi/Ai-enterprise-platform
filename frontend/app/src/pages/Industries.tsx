import { ContentPageTemplate } from '@/components/shared/ContentPageTemplate';
import { industriesPage } from '@/lib/siteContent';

export default function Industries() {
  return <ContentPageTemplate page={industriesPage} />;
}
