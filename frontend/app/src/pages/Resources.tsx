import { ContentPageTemplate } from '@/components/shared/ContentPageTemplate';
import { resourcesPage } from '@/lib/siteContent';

export default function Resources() {
  return <ContentPageTemplate page={resourcesPage} />;
}
