import { ContentPageTemplate } from '@/components/shared/ContentPageTemplate';
import { capabilitiesPage } from '@/lib/siteContent';

export default function Capabilities() {
  return <ContentPageTemplate page={capabilitiesPage} />;
}
