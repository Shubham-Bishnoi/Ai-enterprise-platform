import { ContentPageTemplate } from '@/components/shared/ContentPageTemplate';
import { platformsPage } from '@/lib/siteContent';

export default function Platforms() {
  return <ContentPageTemplate page={platformsPage} />;
}
