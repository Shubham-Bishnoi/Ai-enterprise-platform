# Production frontend configuration (Vercel).
# Set NEXT_PUBLIC_API_BASE_URL in the Vercel project's Environment Variables.
# Public value only — never place provider keys or backend secrets here.
NEXT_PUBLIC_API_BASE_URL=https://gff-ai-backend.onrender.com
